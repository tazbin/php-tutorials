$ git -- version

$ git config --global user.name "tazbin"
$ git config --global user.email "md.tazbinur.rahaman@gmail.com"

$ git config --list

pwd
cd ~
cd ..
ls
cd desktop/tune

$ git init

$ git add .
$ git commit -m "my first commit"

$ git log
$ git log --author="tazbin"

$ git status

// working copy > statging area > repository //

$ git diff
$ git diff --staged

$ git rm third.txt
//afer removing a file, commit that state MUST!

$ git rv second.txt pudding.txt

$ git commit -am "directly commited to repo"

$ git checkout -- index.html

$ git reset HEAD profile.html

$ git checkout 61bgd -- index.html

$ git remote add <repo_name> <repo_link>
$ git remote

$ git push -u <repo_name>

clear